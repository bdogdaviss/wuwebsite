# Database
DATABASE_URL=postgres://wakeup:wakeup_dev@localhost:5432/wakeup?sslmode=disable

# MinIO / S3
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=wakeup
MINIO_SECRET_KEY=wakeup_dev
MINIO_USE_SSL=false
MINIO_BUCKET=wakeup

# Server
PORT=8080

# Auth
JWT_SECRET=change-this-in-production-use-a-strong-secret

# CORS - comma-separated list of allowed origins (localhost always included)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
